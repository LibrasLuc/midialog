import { media } from "@/lib/data";import { Poster } from "@/components/media-card";import { Eyebrow } from "@/components/ui";
const lists=["Things that rewired my brain","For a rainy night at 2AM","My childhood in ten objects","I would defend these forever"];
export default function Lists(){return <><div className="page-top"><Eyebrow>LISTS WITHOUT BORDERS</Eyebrow><h1>Mix the things you love.</h1><p>Films beside footballers. Books beside games. No categories—just meaning.</p></div><section className="section list-grid">{lists.map((x,i)=><article className="hybrid-list" key={x}><Eyebrow>HYBRID LIST / {12+i*3} ENTRIES</Eyebrow><h2>{x}</h2><p>by @{["caio","lucas","bia","marina.exe"][i]}</p><div className="mini-stack">{media.slice(i,i+4).map(m=><Poster item={m} key={m.id}/>)}</div></article>)}</section></>}
