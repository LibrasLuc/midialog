"use client";
import { useState } from "react"; import { media, typeLabel } from "@/lib/data"; import { MediaCard } from "@/components/media-card"; import { Eyebrow } from "@/components/ui"; import { MediaType } from "@/types";
export default function Explore(){const [filter,setFilter]=useState<MediaType|"ALL">("ALL"); const items=filter==="ALL"?media:media.filter(x=>x.type===filter);return <><div className="page-top"><Eyebrow>BROWSE THE ARCHIVE</Eyebrow><h1>Explore culture.</h1><p>Follow the signal through cinema, television, games, books and football.</p><div className="filters"><button className={`chip ${filter==="ALL"?"active":""}`} onClick={()=>setFilter("ALL")}>ALL SIGNALS</button>{Object.entries(typeLabel).map(([k,v])=><button key={k} onClick={()=>setFilter(k as MediaType)} className={`chip ${filter===k?"active":""}`}>{v.toUpperCase()}</button>)}</div></div><section className="section"><div className="media-grid">{items.map(x=><MediaCard item={x} key={x.id}/>)}</div></section></>}
