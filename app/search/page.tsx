"use client";
import { useState } from "react"; import { Search } from "lucide-react"; import { media } from "@/lib/data"; import { MediaCard } from "@/components/media-card"; import { Eyebrow } from "@/components/ui";
export default function SearchPage(){const [q,setQ]=useState("");const result=media.filter(x=>(x.title+x.creator+x.tags.join(" ")).toLowerCase().includes(q.toLowerCase()));return <><div className="page-top"><Eyebrow>GLOBAL SEARCH / CTRL + K</Eyebrow><h1>Find your next obsession.</h1><div className="searchbox"><Search/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Search films, games, books, players, people..."/><span className="chip">ESC</span></div></div><section className="section">{q&&<p className="eyebrow">{result.length} SIGNALS FOUND FOR “{q.toUpperCase()}”</p>}<div className="media-grid">{result.map(x=><MediaCard item={x} key={x.id}/>)}</div></section></>}
